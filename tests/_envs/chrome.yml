# `chrome` environment config goes here
modules:
  config:
    Concrete5Browser:
      browser: chrome
      port: 4444
      capabilities:
        "goog:chromeOptions":
          args:
            - "--headless"
            - window-size=1920,1080
        window_size: false